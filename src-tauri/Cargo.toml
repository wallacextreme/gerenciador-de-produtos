[package]
name = "gestaopro"
version = "1.13.0"
description = "GestãoPro — Gestão Comercial, Estoque e PDV"
authors = ["GestãoPro Team"]
edition = "2021"

[lib]
name = "gestaopro_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
